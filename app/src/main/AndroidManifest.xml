<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Ads only. The app's own data never leaves the device. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      localeConfig is read by Android 13 and later to list the app in the system language picker.
      Older versions ignore it and take their language from the in-app setting instead, which is
      why lint is told not to flag it as unused below the minimum.
    -->
    <application
        android:name=".PokerTrackerApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:localeConfig="@xml/locales_config"
        android:theme="@style/Theme.PokerTracker"
        tools:ignore="UnusedAttribute">
        <!-- Filled in by app/build.gradle.kts: ADMOB_APP_ID, or Google's sample app ID. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="${admobAppId}" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.PokerTracker">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>